// Phase 2 — Organizer extension.
// Shows side-by-side structured field comparison across selected statutes.
// Requires: multi-select in ResultsPanel, POST /compare endpoint (Person 4).
export default function ComparisonTable() {
  return null;
}
