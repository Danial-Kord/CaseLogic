// Phase 2 feature.
// In Phase 2, when OpenClaw generates prose answers, this panel shows
// per-claim verification badges (verified / partial / unsupported) and
// source snippets. Phase 1 answers are retrieved statutes — each result
// IS the source — so this panel is structurally unnecessary until then.
// See: POST /verify endpoint, verify_claims() in baseline architecture §9.
export default function VerificationPanel() {
  return null;
}
